name: Deploy Worker to Cloudflare

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

# One deploy at a time; let an in-progress deploy finish.
concurrency:
  group: worker-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Deploys worker/index.js per wrangler.toml. No build/install step: the
      # Worker is plain JS and wrangler-action brings its own wrangler.
      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
          # This repo pins Yarn Berry via package.json "packageManager", which the
          # action's default yarn shell-out can't satisfy on the runner (global
          # Yarn is 1.x, Corepack off). npm installs/runs wrangler without that.
          packageManager: npm
